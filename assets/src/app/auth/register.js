angular.module( 'sailng.register', [
])


    .controller( 'RegisterCtrl', function ItemListController( $scope, $sails, lodash, config, titleService ,$filter ) {
        console.log("register ctrl");
        $scope.items = [];
       
    });
